cat /etc/passwd | sed '/^#/d' | awk 'NR % 2 == 0' | awk -F ":" '{  print $1 }' | rev | sort -fnr | awk -v min="$FT_LINE1" -v max="$FT_LINE2" "NR >= min && NR <= max" | tr '\n' ' '  | sed 's/ /, /g' | sed 's/, $/./g' | tr -d '\n'
